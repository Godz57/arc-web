# ARC WEB — variáveis de ambiente
# Copie este arquivo para .env.local e preencha os valores necessários.

# URL pública do site (usada em metadata, sitemap e robots)
NEXT_PUBLIC_SITE_URL=https://arc-web.vercel.app

# Endpoint para recebimento de mensagens do formulário de contato (futuro backend)
# CONTACT_API_ENDPOINT=
